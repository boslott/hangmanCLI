var word = {
  answer: 'hang man',
  letters: [
    {
      letter: 'h',
      count: 1,
      guess: false
    }
  ]
}

function Letter(char) {
this.visible = !/[a-z]/i.test(char);
this.char = char;
}

letter.prototype.toString
  if visible return char
  else return '_';

  letter.prototype.guess = function(charGuess)
    if charGuess.toUpperCase() === this.char.toUpperCase()
      visible = 1;



function

toString
getSolution
guessLetter
